apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: gitlab-operator-policy-subscription
  namespace: policies
  labels:
    app: gitlab-operator-policy
  annotations:
    apps.open-cluster-management.io/git-branch: main
    apps.open-cluster-management.io/git-path: policies/gitlab-operator
    apps.open-cluster-management.io/reconcile-option: merge
spec:
  channel: acm-operator-policy-demo-ns/acm-operator-policy-demo
  placement:
    placementRef:
      kind: Placement
      name: hub-cluster-placement
