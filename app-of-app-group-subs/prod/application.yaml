apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: gitlab-prod-group-apps
  namespace: gitlab-prod
  labels:
    app: gitlab-prod-group-apps
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - gitlab-prod-group-apps
