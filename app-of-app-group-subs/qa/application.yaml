apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: gitlab-qa-group-apps
  namespace: gitlab-qa
  labels:
    app: gitlab-qa-group-apps
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - gitlab-qa-group-apps
